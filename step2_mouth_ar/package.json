{
  "name": "dental-ar-step2-mouth-tracking",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Steps 2-3 of AR Based Dental Imaging: real-time face/mouth tracking, AR anchor, and tooth detection/tracking",
  "scripts": {
    "postinstall": "node scripts/vendor-wasm.mjs && node scripts/fetch-model.mjs",
    "setup": "npm run vendor:wasm && npm run fetch:model",
    "dev": "vite --host",
    "dev:https": "vite --host --mode https",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test tests/*.test.mjs",
    "vendor:wasm": "node scripts/vendor-wasm.mjs",
    "fetch:model": "node scripts/fetch-model.mjs",
    "cap:sync": "npm run build && cap sync",
    "cap:android": "npm run build && cap sync android && cap open android",
    "cap:ios": "npm run build && cap sync ios && cap open ios"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.14"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "@vitejs/plugin-basic-ssl": "^1.1.0"
  }
}
